import { _ as _export_sfc, s as serverRenderer_cjs_prodExports, v as vueExports, u as useNuxtApp, d as useState, f as useAsyncData, t as tryUseNuxtApp } from './server.mjs';
import { _ as __nuxt_component_0$1 } from './nuxt-link-BdW3uYUq.mjs';
import { ChevronDown, ArrowRight, X } from 'file:///Users/zhuxiaoming/Documents/trae_projects/template01/node_modules/.pnpm/lucide-vue-next@0.577.0_vue@3.5.30_typescript@5.9.3_/node_modules/lucide-vue-next/dist/cjs/lucide-vue-next.js';
import { withoutTrailingSlash } from 'file:///Users/zhuxiaoming/Documents/trae_projects/template01/node_modules/.pnpm/ufo@1.6.3/node_modules/ufo/dist/index.mjs';
import { pascalCase } from 'file:///Users/zhuxiaoming/Documents/trae_projects/template01/node_modules/.pnpm/scule@1.3.0/node_modules/scule/dist/index.mjs';
import { getRequestHeaders } from 'file:///Users/zhuxiaoming/Documents/trae_projects/template01/node_modules/.pnpm/h3@1.15.6/node_modules/h3/dist/index.mjs';
import 'file:///Users/zhuxiaoming/Documents/trae_projects/template01/node_modules/.pnpm/ofetch@1.5.1/node_modules/ofetch/dist/node.mjs';
import '../nitro/nitro.mjs';
import 'file:///Users/zhuxiaoming/Documents/trae_projects/template01/node_modules/.pnpm/destr@2.0.5/node_modules/destr/dist/index.mjs';
import 'file:///Users/zhuxiaoming/Documents/trae_projects/template01/node_modules/.pnpm/hookable@5.5.3/node_modules/hookable/dist/index.mjs';
import 'file:///Users/zhuxiaoming/Documents/trae_projects/template01/node_modules/.pnpm/node-mock-http@1.0.4/node_modules/node-mock-http/dist/index.mjs';
import 'file:///Users/zhuxiaoming/Documents/trae_projects/template01/node_modules/.pnpm/unstorage@1.17.4_db0@0.3.4_better-sqlite3@12.8.0__ioredis@5.10.0/node_modules/unstorage/dist/index.mjs';
import 'file:///Users/zhuxiaoming/Documents/trae_projects/template01/node_modules/.pnpm/unstorage@1.17.4_db0@0.3.4_better-sqlite3@12.8.0__ioredis@5.10.0/node_modules/unstorage/drivers/fs.mjs';
import 'node:crypto';
import 'file:///Users/zhuxiaoming/Documents/trae_projects/template01/node_modules/.pnpm/unstorage@1.17.4_db0@0.3.4_better-sqlite3@12.8.0__ioredis@5.10.0/node_modules/unstorage/drivers/fs-lite.mjs';
import 'file:///Users/zhuxiaoming/Documents/trae_projects/template01/node_modules/.pnpm/unstorage@1.17.4_db0@0.3.4_better-sqlite3@12.8.0__ioredis@5.10.0/node_modules/unstorage/drivers/lru-cache.mjs';
import 'file:///Users/zhuxiaoming/Documents/trae_projects/template01/node_modules/.pnpm/ohash@2.0.11/node_modules/ohash/dist/index.mjs';
import 'file:///Users/zhuxiaoming/Documents/trae_projects/template01/node_modules/.pnpm/klona@2.0.6/node_modules/klona/dist/index.mjs';
import 'file:///Users/zhuxiaoming/Documents/trae_projects/template01/node_modules/.pnpm/defu@6.1.4/node_modules/defu/dist/defu.mjs';
import 'file:///Users/zhuxiaoming/Documents/trae_projects/template01/node_modules/.pnpm/unctx@2.5.0/node_modules/unctx/dist/index.mjs';
import 'file:///Users/zhuxiaoming/Documents/trae_projects/template01/node_modules/.pnpm/radix3@1.1.2/node_modules/radix3/dist/index.mjs';
import 'file:///Users/zhuxiaoming/Documents/trae_projects/template01/node_modules/.pnpm/vue@3.5.30_typescript@5.9.3/node_modules/vue/index.mjs';
import 'node:fs';
import 'node:url';
import 'file:///Users/zhuxiaoming/Documents/trae_projects/template01/node_modules/.pnpm/pathe@2.0.3/node_modules/pathe/dist/index.mjs';
import 'file:///Users/zhuxiaoming/Documents/trae_projects/template01/node_modules/.pnpm/consola@3.4.2/node_modules/consola/dist/index.mjs';
import 'file:///Users/zhuxiaoming/Documents/trae_projects/template01/node_modules/.pnpm/fast-xml-parser@5.5.6/node_modules/fast-xml-parser/src/fxp.js';
import 'file:///Users/zhuxiaoming/Documents/trae_projects/template01/node_modules/.pnpm/db0@0.3.4_better-sqlite3@12.8.0/node_modules/db0/dist/connectors/better-sqlite3.mjs';
import 'file:///Users/zhuxiaoming/Documents/trae_projects/template01/node_modules/.pnpm/hookable@6.1.0/node_modules/hookable/dist/index.mjs';
import 'file:///Users/zhuxiaoming/Documents/trae_projects/template01/node_modules/.pnpm/perfect-debounce@2.1.0/node_modules/perfect-debounce/dist/index.mjs';
import 'node:stream';

function defaultEstimatedProgress(duration, elapsed) {
  const completionPercentage = elapsed / duration * 100;
  return 2 / Math.PI * 100 * Math.atan(completionPercentage / 50);
}
function createLoadingIndicator(opts = {}) {
  const { duration = 2e3, throttle = 200, hideDelay = 500, resetDelay = 400 } = opts;
  opts.estimatedProgress || defaultEstimatedProgress;
  const nuxtApp = useNuxtApp();
  const progress = vueExports.shallowRef(0);
  const isLoading = vueExports.shallowRef(false);
  const error = vueExports.shallowRef(false);
  const start = (opts2 = {}) => {
    error.value = false;
    set(0, opts2);
  };
  function set(at = 0, opts2 = {}) {
    if (nuxtApp.isHydrating) {
      return;
    }
    if (at >= 100) {
      return finish({ force: opts2.force });
    }
    progress.value = at < 0 ? 0 : at;
    opts2.force ? 0 : throttle;
    {
      isLoading.value = true;
    }
  }
  function finish(opts2 = {}) {
    progress.value = 100;
    if (opts2.error) {
      error.value = true;
    }
    if (opts2.force) {
      progress.value = 0;
      isLoading.value = false;
    }
  }
  function clear() {
  }
  let _cleanup = () => {
  };
  return {
    _cleanup,
    progress: vueExports.computed(() => progress.value),
    isLoading: vueExports.computed(() => isLoading.value),
    error: vueExports.computed(() => error.value),
    start,
    set,
    finish,
    clear
  };
}
function useLoadingIndicator(opts = {}) {
  const nuxtApp = useNuxtApp();
  const indicator = nuxtApp._loadingIndicator ||= createLoadingIndicator(opts);
  return indicator;
}
const __nuxt_component_0 = vueExports.defineComponent({
  name: "NuxtLoadingIndicator",
  props: {
    throttle: {
      type: Number,
      default: 200
    },
    duration: {
      type: Number,
      default: 2e3
    },
    hideDelay: {
      type: Number,
      default: 500
    },
    resetDelay: {
      type: Number,
      default: 400
    },
    height: {
      type: Number,
      default: 3
    },
    color: {
      type: [String, Boolean],
      default: "repeating-linear-gradient(to right,#00dc82 0%,#34cdfe 50%,#0047e1 100%)"
    },
    errorColor: {
      type: String,
      default: "repeating-linear-gradient(to right,#f87171 0%,#ef4444 100%)"
    },
    estimatedProgress: {
      type: Function,
      required: false
    }
  },
  setup(props, { slots, expose }) {
    const { progress, isLoading, error, start, finish, clear } = useLoadingIndicator({
      duration: props.duration,
      throttle: props.throttle,
      hideDelay: props.hideDelay,
      resetDelay: props.resetDelay,
      estimatedProgress: props.estimatedProgress
    });
    expose({
      progress,
      isLoading,
      error,
      start,
      finish,
      clear
    });
    return () => vueExports.h("div", {
      class: "nuxt-loading-indicator",
      style: {
        position: "fixed",
        top: 0,
        right: 0,
        left: 0,
        pointerEvents: "none",
        width: "auto",
        height: `${props.height}px`,
        opacity: isLoading.value ? 1 : 0,
        background: error.value ? props.errorColor : props.color || void 0,
        backgroundSize: `${progress.value > 0 ? 100 / progress.value * 100 : 0}% auto`,
        transform: `scaleX(${progress.value}%)`,
        transformOrigin: "left",
        transition: "transform 0.1s, height 0.4s, opacity 0.4s",
        zIndex: 999999
      }
    }, slots);
  }
});
const checksums = {
  "products": "v3.5.0--N46lrz3Cp9NKY3JoI1crrvpGXqNzdc7w5j_ZOwbmEpc"
};
const tables = {
  "products": "_content_products",
  "info": "_content_info"
};
const buildGroup = (group, type) => {
  const conditions = group._conditions;
  return conditions.length > 0 ? `(${conditions.join(` ${type} `)})` : "";
};
const collectionQueryGroup = (collection) => {
  const conditions = [];
  const query = {
    // @ts-expect-error -- internal
    _conditions: conditions,
    where(field, operator, value) {
      let condition;
      switch (operator.toUpperCase()) {
        case "IN":
        case "NOT IN":
          if (Array.isArray(value)) {
            const values = value.map((val) => singleQuote(val)).join(", ");
            condition = `"${String(field)}" ${operator.toUpperCase()} (${values})`;
          } else {
            throw new TypeError(`Value for ${operator} must be an array`);
          }
          break;
        case "BETWEEN":
        case "NOT BETWEEN":
          if (Array.isArray(value) && value.length === 2) {
            condition = `"${String(field)}" ${operator.toUpperCase()} ${singleQuote(value[0])} AND ${singleQuote(value[1])}`;
          } else {
            throw new Error(`Value for ${operator} must be an array with two elements`);
          }
          break;
        case "IS NULL":
        case "IS NOT NULL":
          condition = `"${String(field)}" ${operator.toUpperCase()}`;
          break;
        case "LIKE":
        case "NOT LIKE":
          condition = `"${String(field)}" ${operator.toUpperCase()} ${singleQuote(value)}`;
          break;
        default:
          condition = `"${String(field)}" ${operator} ${singleQuote(typeof value === "boolean" ? Number(value) : value)}`;
      }
      conditions.push(`${condition}`);
      return query;
    },
    andWhere(groupFactory) {
      const group = groupFactory(collectionQueryGroup());
      conditions.push(buildGroup(group, "AND"));
      return query;
    },
    orWhere(groupFactory) {
      const group = groupFactory(collectionQueryGroup());
      conditions.push(buildGroup(group, "OR"));
      return query;
    }
  };
  return query;
};
const collectionQueryBuilder = (collection, fetch) => {
  const params = {
    conditions: [],
    selectedFields: [],
    offset: 0,
    limit: 0,
    orderBy: [],
    // Count query
    count: {
      field: "",
      distinct: false
    }
  };
  const query = {
    // @ts-expect-error -- internal
    __params: params,
    andWhere(groupFactory) {
      const group = groupFactory(collectionQueryGroup());
      params.conditions.push(buildGroup(group, "AND"));
      return query;
    },
    orWhere(groupFactory) {
      const group = groupFactory(collectionQueryGroup());
      params.conditions.push(buildGroup(group, "OR"));
      return query;
    },
    path(path) {
      return query.where("path", "=", withoutTrailingSlash(path));
    },
    skip(skip) {
      params.offset = skip;
      return query;
    },
    where(field, operator, value) {
      query.andWhere((group) => group.where(String(field), operator, value));
      return query;
    },
    limit(limit) {
      params.limit = limit;
      return query;
    },
    select(...fields) {
      if (fields.length) {
        params.selectedFields.push(...fields);
      }
      return query;
    },
    order(field, direction) {
      params.orderBy.push(`"${String(field)}" ${direction}`);
      return query;
    },
    async all() {
      return fetch(collection, buildQuery()).then((res) => res || []);
    },
    async first() {
      return fetch(collection, buildQuery({ limit: 1 })).then((res) => res[0] || null);
    },
    async count(field = "*", distinct = false) {
      return fetch(collection, buildQuery({
        count: { field: String(field), distinct }
      })).then((m) => m[0].count);
    }
  };
  function buildQuery(opts = {}) {
    let query2 = "SELECT ";
    if (opts?.count) {
      query2 += `COUNT(${opts.count.distinct ? "DISTINCT " : ""}${opts.count.field}) as count`;
    } else {
      const fields = Array.from(new Set(params.selectedFields));
      query2 += fields.length > 0 ? fields.map((f) => `"${String(f)}"`).join(", ") : "*";
    }
    query2 += ` FROM ${tables[String(collection)]}`;
    if (params.conditions.length > 0) {
      query2 += ` WHERE ${params.conditions.join(" AND ")}`;
    }
    if (params.orderBy.length > 0) {
      query2 += ` ORDER BY ${params.orderBy.join(", ")}`;
    } else {
      query2 += ` ORDER BY stem ASC`;
    }
    const limit = opts?.limit || params.limit;
    if (limit > 0) {
      if (params.offset > 0) {
        query2 += ` LIMIT ${limit} OFFSET ${params.offset}`;
      } else {
        query2 += ` LIMIT ${limit}`;
      }
    }
    return query2;
  }
  return query;
};
function singleQuote(value) {
  return `'${String(value).replace(/'/g, "''")}'`;
}
function pick(keys) {
  return (obj) => {
    obj = obj || {};
    return (keys || []).filter((key) => typeof obj[key] !== "undefined").reduce((newObj, key) => Object.assign(newObj, { [key]: obj[key] }), {});
  };
}
async function generateNavigationTree(queryBuilder, extraFields = []) {
  const params = queryBuilder.__params;
  if (!params?.orderBy?.length) {
    queryBuilder = queryBuilder.order("stem", "ASC");
  }
  const collecitonItems = await queryBuilder.orWhere(
    (group) => group.where("navigation", "<>", "false").where("navigation", "IS NULL")
  ).select("navigation", "stem", "path", "title", "meta", ...extraFields || []).all();
  const { contents, configs } = collecitonItems.reduce((acc, c) => {
    if (String(c.stem).split("/").pop() === ".navigation") {
      c.title = c.title?.toLowerCase() === "navigation" ? "" : c.title;
      const key = c.path.split("/").slice(0, -1).join("/") || "/";
      acc.configs[key] = {
        ...c,
        ...c.body
      };
    } else {
      acc.contents.push(c);
    }
    return acc;
  }, { configs: {}, contents: [] });
  const pickConfigNavigationFields = (content) => ({
    ...pick(["title", ...extraFields])(content),
    ...content.meta,
    ...isObject(content?.navigation) ? content.navigation : {}
  });
  const pickNavigationFields = (content) => ({
    ...pick(["title", ...extraFields])(content),
    ...isObject(content?.navigation) ? content.navigation : {}
  });
  const nav = contents.reduce((nav2, content) => {
    const parts = content.path.substring(1).split("/");
    const idParts = content.stem.split("/");
    const isIndex = !!idParts[idParts.length - 1]?.match(/([1-9]\d*\.)?index/g);
    const getNavItem = (content2) => ({
      title: content2.title,
      path: content2.path,
      stem: content2.stem,
      children: [],
      ...pickNavigationFields(content2)
    });
    const navItem = getNavItem(content);
    if (isIndex) {
      const dirConfig = configs[navItem.path];
      if (typeof dirConfig?.navigation !== "undefined" && dirConfig?.navigation === false) {
        return nav2;
      }
      if (content.path !== "/") {
        const indexItem = getNavItem(content);
        navItem.children.push(indexItem);
      }
      if (dirConfig) {
        Object.assign(
          navItem,
          pickConfigNavigationFields(dirConfig)
        );
      }
    }
    if (parts.length === 1) {
      const existed2 = nav2.find((item) => item.path === navItem.path && item.page === false);
      if (isIndex && existed2) {
        Object.assign(existed2, {
          page: void 0,
          children: [
            ...navItem.children || [],
            ...existed2.children || []
          ]
        });
      } else {
        nav2.push(navItem);
      }
      return nav2;
    }
    const siblings = parts.slice(0, -1).reduce((nodes, part, i) => {
      const currentPathPart = "/" + parts.slice(0, i + 1).join("/");
      const conf = configs[currentPathPart];
      if (typeof conf?.navigation !== "undefined" && conf.navigation === false) {
        return [];
      }
      let parent = nodes.find((n) => n.path === currentPathPart);
      if (!parent) {
        const navigationConfig = conf ? pickConfigNavigationFields(conf) : {};
        parent = {
          ...navigationConfig,
          title: navigationConfig.title || generateTitle(part),
          path: currentPathPart,
          stem: idParts.slice(0, i + 1).join("/"),
          children: [],
          page: false
        };
        nodes.push(parent);
      }
      return parent.children;
    }, nav2);
    const existed = siblings.find((item) => item.path === navItem.path && item.page === false);
    if (existed) {
      Object.assign(existed, {
        ...navItem,
        page: void 0,
        children: [
          ...navItem.children || [],
          ...existed.children || []
        ]
      });
    } else {
      siblings.push(navItem);
    }
    return nav2;
  }, []);
  return sortAndClear(nav);
}
function sortAndClear(nav) {
  const sorted = nav;
  for (const item of sorted) {
    if (item.children?.length) {
      sortAndClear(item.children);
    } else {
      delete item.children;
    }
  }
  return nav;
}
function isObject(obj) {
  return obj !== null && Object.prototype.toString.call(obj) === "[object Object]";
}
const generateTitle = (path) => path.split(/[\s-]/g).map(pascalCase).join(" ");
async function fetchContent(event, collection, path, options) {
  const headers = event ? getRequestHeaders(event) : {};
  headers["accept-encoding"] = void 0;
  const url = `/__nuxt_content/${collection}/${path}`;
  const fetchOptions = {
    ...options,
    headers: {
      ...headers,
      ...options.headers
    },
    query: { v: checksums[String(collection)], t: void 0 }
  };
  return event ? await event.$fetch(url, fetchOptions) : await $fetch(url, fetchOptions);
}
async function fetchQuery(event, collection, sql) {
  return fetchContent(event, collection, "query", {
    headers: {
      "content-type": "application/json"
    },
    method: "POST",
    body: {
      sql
    }
  });
}
const queryCollection = (collection) => {
  const event = tryUseNuxtApp()?.ssrContext?.event;
  return collectionQueryBuilder(collection, (collection2, sql) => executeContentQuery(event, collection2, sql));
};
function queryCollectionNavigation(collection, fields) {
  return chainablePromise(collection, (qb) => generateNavigationTree(qb, fields));
}
async function executeContentQuery(event, collection, sql) {
  {
    return fetchQuery(event, String(collection), sql);
  }
}
function chainablePromise(collection, fn) {
  const queryBuilder = queryCollection(collection);
  const chainable = {
    where(field, operator, value) {
      queryBuilder.where(String(field), operator, value);
      return chainable;
    },
    andWhere(groupFactory) {
      queryBuilder.andWhere(groupFactory);
      return chainable;
    },
    orWhere(groupFactory) {
      queryBuilder.orWhere(groupFactory);
      return chainable;
    },
    order(field, direction) {
      queryBuilder.order(String(field), direction);
      return chainable;
    },
    then(onfulfilled, onrejected) {
      return fn(queryBuilder).then(onfulfilled, onrejected);
    },
    catch(onrejected) {
      return this.then(void 0, onrejected);
    },
    finally(onfinally) {
      return this.then(void 0, void 0).finally(onfinally);
    },
    get [Symbol.toStringTag]() {
      return "Promise";
    }
  };
  return chainable;
}
const useProductsNav = async () => {
  const productsNav = useState("products-nav", () => []);
  if (productsNav.value && productsNav.value.length > 0) {
    return { data: productsNav };
  }
  const { data } = await useAsyncData(
    "products-nav",
    () => queryCollectionNavigation("products")
  );
  productsNav.value = data.value || [];
  return { data: productsNav };
};
const _sfc_main$1 = /* @__PURE__ */ vueExports.defineComponent({
  __name: "navbar",
  __ssrInlineRender: true,
  async setup(__props) {
    let __temp, __restore;
    const isScrolled = vueExports.ref(false);
    const mobileMenuOpen = vueExports.ref(false);
    const mobileDropdowns = vueExports.ref({});
    const siteConfig = vueExports.ref({
      logoName: "",
      logoUrl: ""
    });
    const { data: navData } = ([__temp, __restore] = vueExports.withAsyncContext(() => useProductsNav()), __temp = await __temp, __restore(), __temp);
    const navItems = vueExports.computed(() => {
      const rootNode = navData.value?.[0];
      if (!rootNode) {
        return [
          { label: "Home", href: "/" },
          { label: "About", href: "/about" },
          { label: "Contact", href: "/contact" }
        ];
      }
      const productMenu = {
        label: rootNode.title,
        // "Products"
        href: rootNode.path,
        // "/products"
        hasDropdown: true,
        subItems: rootNode.children?.map((child) => ({
          label: child.title,
          // "Industrial"
          href: child.path
          // "/products/industrial"
        })) || []
      };
      return [
        { label: "Home", href: "/" },
        productMenu,
        { label: "About", href: "/about" },
        { label: "Contact", href: "/contact" }
      ];
    });
    vueExports.watch(mobileMenuOpen, (isOpen) => {
    });
    return (_ctx, _push, _parent, _attrs) => {
      const _component_NuxtLink = __nuxt_component_0$1;
      _push(`<nav${serverRenderer_cjs_prodExports.ssrRenderAttrs(vueExports.mergeProps({
        class: [
          "fixed top-0 left-0 right-0 z-[100] transition-all duration-500 ease-[cubic-bezier(0.25,1,0.5,1)]",
          isScrolled.value ? "bg-[#001151]/85 backdrop-blur-xl border-b border-white/10 shadow-lg " : "bg-[#001151] py-2"
        ]
      }, _attrs))} data-v-25606fd9><div class="container mx-auto px-4" data-v-25606fd9><div class="flex items-center justify-between h-20" data-v-25606fd9><div class="${serverRenderer_cjs_prodExports.ssrRenderClass([isScrolled.value ? "scale-90" : "scale-100", "transition-all duration-500"])}" data-v-25606fd9>`);
      _push(serverRenderer_cjs_prodExports.ssrRenderComponent(_component_NuxtLink, {
        to: "/",
        class: "flex items-center gap-3 py-4 group"
      }, {
        default: vueExports.withCtx((_, _push2, _parent2, _scopeId) => {
          if (_push2) {
            _push2(`<div class="flex w-10 h-10 items-center justify-center" data-v-25606fd9${_scopeId}>`);
            if (siteConfig.value?.logo?.logoUrl) {
              _push2(`<img${serverRenderer_cjs_prodExports.ssrRenderAttr("src", siteConfig.value?.logo?.logoUrl)} alt="Company Logo" class="w-full h-full object-contain" data-v-25606fd9${_scopeId}>`);
            } else {
              _push2(`<!---->`);
            }
            _push2(`</div><span class="text-2xl font-extrabold text-white tracking-tight uppercase" data-v-25606fd9${_scopeId}>${serverRenderer_cjs_prodExports.ssrInterpolate(siteConfig.value?.logo?.logoName)}</span>`);
          } else {
            return [
              vueExports.createVNode("div", { class: "flex w-10 h-10 items-center justify-center" }, [
                siteConfig.value?.logo?.logoUrl ? (vueExports.openBlock(), vueExports.createBlock("img", {
                  key: 0,
                  src: siteConfig.value?.logo?.logoUrl,
                  alt: "Company Logo",
                  class: "w-full h-full object-contain"
                }, null, 8, ["src"])) : vueExports.createCommentVNode("", true)
              ]),
              vueExports.createVNode("span", { class: "text-2xl font-extrabold text-white tracking-tight uppercase" }, vueExports.toDisplayString(siteConfig.value?.logo?.logoName), 1)
            ];
          }
        }),
        _: 1
      }, _parent));
      _push(`</div><div class="hidden md:flex items-center h-full space-x-1 lg:space-x-4" data-v-25606fd9><!--[-->`);
      serverRenderer_cjs_prodExports.ssrRenderList(vueExports.unref(navItems), (item, index) => {
        _push(`<div class="relative group h-full flex items-center" data-v-25606fd9>`);
        _push(serverRenderer_cjs_prodExports.ssrRenderComponent(_component_NuxtLink, {
          to: item.href,
          class: "text-white/90 hover:text-blue-400 transition-all px-4 py-2 rounded-md hover:bg-white/5 font-medium text-sm lg:text-base flex items-center gap-1"
        }, {
          default: vueExports.withCtx((_, _push2, _parent2, _scopeId) => {
            if (_push2) {
              _push2(`${serverRenderer_cjs_prodExports.ssrInterpolate(item.label)} `);
              if (item.hasDropdown) {
                _push2(serverRenderer_cjs_prodExports.ssrRenderComponent(vueExports.unref(ChevronDown), { class: "w-4 h-4 opacity-50 group-hover:rotate-180 transition-transform duration-300" }, null, _parent2, _scopeId));
              } else {
                _push2(`<!---->`);
              }
            } else {
              return [
                vueExports.createTextVNode(vueExports.toDisplayString(item.label) + " ", 1),
                item.hasDropdown ? (vueExports.openBlock(), vueExports.createBlock(vueExports.unref(ChevronDown), {
                  key: 0,
                  class: "w-4 h-4 opacity-50 group-hover:rotate-180 transition-transform duration-300"
                })) : vueExports.createCommentVNode("", true)
              ];
            }
          }),
          _: 2
        }, _parent));
        if (item.hasDropdown) {
          _push(`<div class="absolute top-[100%] left-0 w-48 bg-white/95 backdrop-blur-sm text-slate-800 rounded-xl shadow-2xl py-2 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-300 transform translate-y-4 group-hover:translate-y-1 border border-slate-100/50 z-[200]" data-v-25606fd9><div class="absolute -top-4 left-0 w-full h-4" data-v-25606fd9></div><!--[-->`);
          serverRenderer_cjs_prodExports.ssrRenderList(item.subItems, (subItem, subIndex) => {
            _push(serverRenderer_cjs_prodExports.ssrRenderComponent(_component_NuxtLink, {
              key: subIndex,
              to: subItem.href,
              class: "block px-5 py-3 text-sm hover:bg-blue-50 hover:text-blue-700 transition-colors border-b border-slate-50 last:border-b-0 font-medium"
            }, {
              default: vueExports.withCtx((_, _push2, _parent2, _scopeId) => {
                if (_push2) {
                  _push2(`${serverRenderer_cjs_prodExports.ssrInterpolate(subItem.label)}`);
                } else {
                  return [
                    vueExports.createTextVNode(vueExports.toDisplayString(subItem.label), 1)
                  ];
                }
              }),
              _: 2
            }, _parent));
          });
          _push(`<!--]--></div>`);
        } else {
          _push(`<!---->`);
        }
        _push(`</div>`);
      });
      _push(`<!--]--></div><div class="hidden md:flex items-center" data-v-25606fd9>`);
      _push(serverRenderer_cjs_prodExports.ssrRenderComponent(_component_NuxtLink, {
        to: "/contact",
        class: "relative inline-flex items-center gap-2 px-7 py-3 rounded-md font-bold transition-colors duration-300 bg-white text-[#001151] hover:bg-slate-200/90 active:scale-[0.98]"
      }, {
        default: vueExports.withCtx((_, _push2, _parent2, _scopeId) => {
          if (_push2) {
            _push2(`<span class="text-sm uppercase tracking-wider" data-v-25606fd9${_scopeId}>Inquiry Now</span>`);
            _push2(serverRenderer_cjs_prodExports.ssrRenderComponent(vueExports.unref(ArrowRight), { class: "w-4 h-4 transition-transform duration-300 group-hover:translate-x-1" }, null, _parent2, _scopeId));
          } else {
            return [
              vueExports.createVNode("span", { class: "text-sm uppercase tracking-wider" }, "Inquiry Now"),
              vueExports.createVNode(vueExports.unref(ArrowRight), { class: "w-4 h-4 transition-transform duration-300 group-hover:translate-x-1" })
            ];
          }
        }),
        _: 1
      }, _parent));
      _push(`</div><div class="md:hidden flex items-center" data-v-25606fd9><button class="text-white p-2 rounded-lg hover:bg-white/10 transition-colors focus:outline-none" aria-label="Toggle Menu" data-v-25606fd9>`);
      if (mobileMenuOpen.value) {
        _push(serverRenderer_cjs_prodExports.ssrRenderComponent(vueExports.unref(X), { class: "w-8 h-8" }, null, _parent));
      } else {
        _push(`<svg xmlns="http://www.w3.org/2000/svg" class="w-8 h-8" fill="none" viewBox="0 0 24 24" stroke="currentColor" data-v-25606fd9><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7" data-v-25606fd9></path></svg>`);
      }
      _push(`</button></div>`);
      if (mobileMenuOpen.value) {
        _push(`<div class="md:hidden fixed top-22 left-0 right-0 bottom-0 z-[99] bg-[#001151] backdrop-blur-xl overflow-y-auto" data-v-25606fd9><div class="container mx-auto px-6" data-v-25606fd9><!--[-->`);
        serverRenderer_cjs_prodExports.ssrRenderList(vueExports.unref(navItems), (item, index) => {
          _push(`<div class="border-b border-white/10 last:border-0" data-v-25606fd9><div class="flex items-center justify-between py-5" data-v-25606fd9>`);
          _push(serverRenderer_cjs_prodExports.ssrRenderComponent(_component_NuxtLink, {
            to: item.href,
            onClick: ($event) => mobileMenuOpen.value = false,
            class: "text-white hover:text-blue-300 font-bold text-xl transition-colors"
          }, {
            default: vueExports.withCtx((_, _push2, _parent2, _scopeId) => {
              if (_push2) {
                _push2(`${serverRenderer_cjs_prodExports.ssrInterpolate(item.label)}`);
              } else {
                return [
                  vueExports.createTextVNode(vueExports.toDisplayString(item.label), 1)
                ];
              }
            }),
            _: 2
          }, _parent));
          if (item.hasDropdown) {
            _push(`<button class="p-2 text-white/50" data-v-25606fd9>`);
            _push(serverRenderer_cjs_prodExports.ssrRenderComponent(vueExports.unref(ChevronDown), {
              class: [{ "rotate-180": mobileDropdowns.value[index] }, "w-6 h-6 transition-transform"]
            }, null, _parent));
            _push(`</button>`);
          } else {
            _push(`<!---->`);
          }
          _push(`</div>`);
          if (item.hasDropdown && mobileDropdowns.value[index]) {
            _push(`<div class="pb-6 pl-4 space-y-4" data-v-25606fd9><!--[-->`);
            serverRenderer_cjs_prodExports.ssrRenderList(item.subItems, (subItem, subIndex) => {
              _push(serverRenderer_cjs_prodExports.ssrRenderComponent(_component_NuxtLink, {
                key: subIndex,
                to: subItem.href,
                onClick: ($event) => mobileMenuOpen.value = false,
                class: "block text-white/70 hover:text-white font-medium text-lg border-l-2 border-white/20 pl-4 py-1"
              }, {
                default: vueExports.withCtx((_, _push2, _parent2, _scopeId) => {
                  if (_push2) {
                    _push2(`${serverRenderer_cjs_prodExports.ssrInterpolate(subItem.label)}`);
                  } else {
                    return [
                      vueExports.createTextVNode(vueExports.toDisplayString(subItem.label), 1)
                    ];
                  }
                }),
                _: 2
              }, _parent));
            });
            _push(`<!--]--></div>`);
          } else {
            _push(`<!---->`);
          }
          _push(`</div>`);
        });
        _push(`<!--]--><div class="mt-10 w-full" data-v-25606fd9>`);
        _push(serverRenderer_cjs_prodExports.ssrRenderComponent(_component_NuxtLink, {
          to: "/contact",
          onClick: ($event) => mobileMenuOpen.value = false,
          class: "flex w-full items-center justify-center bg-white text-[#001151] py-4 rounded-xl font-bold text-lg active:scale-[0.98] transition-transform"
        }, {
          default: vueExports.withCtx((_, _push2, _parent2, _scopeId) => {
            if (_push2) {
              _push2(` Inquiry Now `);
            } else {
              return [
                vueExports.createTextVNode(" Inquiry Now ")
              ];
            }
          }),
          _: 1
        }, _parent));
        _push(`</div></div></div>`);
      } else {
        _push(`<!---->`);
      }
      _push(`</div></div></nav>`);
    };
  }
});
const _sfc_setup$1 = _sfc_main$1.setup;
_sfc_main$1.setup = (props, ctx) => {
  const ssrContext = vueExports.useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("components/header/navbar.vue");
  return _sfc_setup$1 ? _sfc_setup$1(props, ctx) : void 0;
};
const __nuxt_component_1 = /* @__PURE__ */ Object.assign(_export_sfc(_sfc_main$1, [["__scopeId", "data-v-25606fd9"]]), { __name: "HeaderNavbar" });
const _sfc_main = {};
function _sfc_ssrRender(_ctx, _push, _parent, _attrs) {
  const _component_NuxtLoadingIndicator = __nuxt_component_0;
  const _component_HeaderNavbar = __nuxt_component_1;
  _push(`<!--[-->`);
  _push(serverRenderer_cjs_prodExports.ssrRenderComponent(_component_NuxtLoadingIndicator, {
    duration: 1e3,
    throttle: 0,
    height: 5
  }, null, _parent));
  _push(serverRenderer_cjs_prodExports.ssrRenderComponent(_component_HeaderNavbar, null, null, _parent));
  _push(`<div class="mt-24" data-v-d379200d>`);
  serverRenderer_cjs_prodExports.ssrRenderSlot(_ctx.$slots, "default", {}, null, _push, _parent);
  _push(`</div><!--]-->`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = vueExports.useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("layouts/default.vue");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _default = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender], ["__scopeId", "data-v-d379200d"]]);

export { _default as default };
//# sourceMappingURL=default-CVzaSJdA.mjs.map
